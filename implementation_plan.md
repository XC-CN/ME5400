# ME5400 联合后端优化方案总览

> 本文档整合了两个版本的升级方案，并说明了它们的区别与取舍关系。

---

## 一、现有架构（Before）

现在的系统是 **"接力赛"模式** —— 每个模块各干各的，通过 ROS topic 传递信息：

```
┌──────────────┐     ┌──────────────┐     ┌──────────────┐
│  PointPillars │     │   FAST-LIO   │     │   MCTrack    │
│  (3D 检测)    │     │  (里程计)     │     │  (跟踪)      │
└──────┬───────┘     └──────┬───────┘     └──────┬───────┘
       │ /kitti/detections   │ /Odometry           │ /mctrack/tracked_objects
       └────────────────────┴────────────────────┘
                       各模块独立运行，只传结果，不联合优化
```

**现有问题**

| 问题 | 说明 |
|------|------|
| **各干各的** | 模块间只传结果，不共享中间状态 |
| **无法回头修正** | IESKF 算出的位姿一旦输出就定了 |
| **动态物体只被降权** | 动态车辆被当"噪声"处理，没有被利用 |
| **单向信息流** | MCTrack 结果只能降权，不能修正位姿 |

---

## 二、两个升级方案对比

### 方案 A：完整联合优化（Object-Centric 因子图）

**核心思路**：自车位姿和目标位姿**全部作为优化变量**，在一个大因子图中联合求解。

| 项目 | 内容 |
|------|------|
| **新增节点** | `graph_optimizer_node` |
| **优化变量** | 自车位姿 X + 目标位姿 O（全部优化） |
| **输出** | `/optimized_odom` + `/optimized_tracks` |
| **因子类型** | 里程计、检测、运动模型、**刚体约束**（创新点） |
| **实现难度** | 高（需要目标节点边缘化、自定义因子雅可比） |
| **计算量** | 大（变量维度 = N×6 + K×N×6，K 为目标数） |
| **创新价值** | 高（联合优化自车+目标，刚体约束首次在 LiDAR 实现） |
| **预估工期** | 4~6 周 |

**因子图结构**

```
时刻 T-2          时刻 T-1          时刻 T
  ⚪ x_{t-2} ——■—— ⚪ x_{t-1} ——■—— ⚪ x_t      ← 自车位姿（优化变量）
      │检测           │检测           │检测
  🔴 o1_{t-2} ─■─ 🔴 o1_{t-1} ─■─ 🔴 o1_t      ← 目标位姿（也是优化变量）
               运动模型            运动模型
      ■ 刚体约束       ■ 刚体约束      ■ 刚体约束  ← 创新点
```

---

### 方案 B：Ego-Only 滑窗优化（Moving Anchor）✅ 采纳版

**核心思路**：只优化自车位姿，目标状态当**已知观测输入**（移动锚点），不参与优化。

| 项目 | 内容 |
|------|------|
| **新增节点** | `joint_backend_ego_node` |
| **优化变量** | 仅自车位姿 X（N×6 维） |
| **目标状态** | 已知观测输入，不优化 |
| **输出** | 仅 `/joint_backend/odom` |
| **因子类型** | 里程计一致性 + 目标观测一致性 |
| **实现难度** | 中（全用 GTSAM 标准因子，无需自定义雅可比） |
| **计算量** | 小（N=10 时仅 60 维） |
| **创新价值** | 中（Moving Landmarks 辅助 ego 定位，思想清晰简洁） |
| **预估工期** | 2~3 周 |

**因子图结构**

```
时刻 T-2              时刻 T-1              时刻 T
  ⚪ x_{t-2} ────■──── ⚪ x_{t-1} ────■──── ⚪ x_t   ← 自车位姿（优化变量）
       │里程计              │里程计              │
       │                   │                   │
  🟡 Ô1_{t-2}         🟡 Ô1_{t-1}         🟡 Ô1_t   ← 目标位姿（已知，由速度传播）
       │目标观测因子         │目标观测因子         │目标观测因子
       ■                   ■                   ■     ← 目标当锚点约束自车
```

---

### 两方案核心区别

| 维度 | 方案 A（完整联合） | 方案 B（Ego-Only）✅ |
|------|-----------------|-------------------|
| **目标是否优化** | ✅ 是 | ❌ 否（当已知输入） |
| **输出目标轨迹** | ✅ `/optimized_tracks` | ❌ 不输出 |
| **刚体约束** | ✅ 有（创新点） | ❌ 无 |
| **边缘化复杂度** | 高（自车+目标均需边缘化） | 低（只有自车需要边缘化） |
| **GTSAM 自定义因子** | 需要（刚体约束） | 不需要（全用内置因子） |
| **论文表述** | "Joint SLAM+MOT optimization" | "Moving landmarks for ego correction" |
| **工程风险** | 高 | 低 |

> **为什么选方案 B**：在学期项目时间内，方案 B 能更快出结果、更稳定运行，且核心论文贡献（动态物体辅助定位）完整保留。方案 A 可作为未来工作。

---

## 三、方案 B 详细设计

### 3.1 输入与输出

```
输入：
  /Odometry                ← FAST-LIO 里程计（位姿 + 协方差）
  /mctrack/tracked_objects ← MCTrack 跟踪结果（位置 + 速度 + 置信度）

输出：
  /joint_backend/odom      ← 优化后的自车位姿
```

### 3.2 滑动窗口

窗口长度 `N = 8~15 帧`（0.8~1.5 秒）。

$$\text{优化变量：} \{X_{k-N+1},\ ...,\ X_k\}, \quad X_i \in SE(3)$$

### 3.3 两类约束

**里程计一致性 `r_odom`**：优化轨迹相邻帧的相对运动应与 FAST-LIO 一致

$$r_{odom,i} = \log\!\left((\Delta X^{meas}_{i-1,i})^{-1} \cdot X_{i-1}^{-1}X_i\right)$$

**目标观测一致性 `r_obj`**：当前 ego 估计下，"我看到目标的位置"应与实际观测一致

$$\hat{z}_{i,j} = X_i^{-1} \hat{O}_{i,j}, \quad r_{obj,i,j} = p^{obs}_{i,j} - p^{pred}_{i,j}$$

其中 $\hat{O}_{i,j}$ 由 MCTrack 的速度+加速度在窗口内传播（匀加速模型，不优化）。

### 3.4 总优化目标

$$\min_{X} \sum_i \|r_{odom,i}\|^2_{W_{odom}} + \lambda \sum_{i,j} \|r_{obj,i,j}\|^2_{W_{obj,i,j}}$$

- `W_odom`：来自 FAST-LIO `/Odometry` 的协方差
- `W_obj`：由 `detection_score`、`track_length`、运动连续性综合计算
- `λ`：全局平衡权重，从 0.3 开始调参

### 3.5 可靠性门控

| 条件 | 说明 |
|------|------|
| `track_length >= 3` | 避免不稳定的新目标 |
| `detection_score >= 0.5` | 检测置信度门限 |
| 速度变化不过大 | 保证匀加速传播有效 |
| 时间戳差 < 0.2s | 时间同步要求 |

### 3.6 实现工具

| 组件 | 选择 |
|------|------|
| 优化器 | `gtsam.LevenbergMarquardtOptimizer` |
| 里程计因子 | `gtsam.BetweenFactorPose3` |
| 目标观测因子 | `gtsam.CustomFactor`（3 维平移残差） |
| 先验因子 | `gtsam.PriorFactorPose3`（窗口首帧锚定） |
| 语言 | Python |

---

## 四、与现有系统的关系

```
FAST-LIO（不改）──┐  /Odometry
                  ▼
MCTrack（不改）──► joint_backend_ego_node ──► /joint_backend/odom
                  ▲
FAST-LIO 权重优化（保留）── 保证前端质量，与后端不冲突
```

> **权重优化必须保留**：它为 FAST-LIO 提供更干净的初值，好的初值让因子图优化更稳定。

---

## 五、实施顺序

| 阶段 | 内容 | 工期 | 验证 |
|------|------|------|------|
| **阶段 1** | 搭建节点，只加里程计因子 | 1 周 | 输出 ≈ FAST-LIO 原始（基准验证） |
| **阶段 2** | 加目标观测因子 + 门控 | 1 周 | ATE/RPE 提升 |
| **阶段 3** | 调参 + 消融实验 + 多序列评估 | 数天 | 论文级结果 |

**消融实验对比组**

| 配置 | 说明 |
|------|------|
| Baseline | FAST-LIO 原始轨迹 |
| + `r_odom` | 里程计因子图（应与 FAST-LIO 接近） |
| + `r_obj` | 加入目标观测约束（应有提升） |
| + 门控调优 | 优化门控阈值后的最终结果 |

---

## 六、新增文件清单

| 文件 | 用途 |
|------|------|
| `catkin_ws/src/ME5400/scripts/joint_backend_ego_node.py` | 核心优化节点 |
| `catkin_ws/src/ME5400/config/joint_backend.yaml` | 参数配置（λ、窗口大小、门控阈值） |
| `Scripts/joint_backend/run_joint_backend.sh` | 启动脚本 |
| `Scripts/evaluation/evaluate_joint_backend.py` | ATE/RPE 对比评估 |

> **不修改任何现有文件**（FAST-LIO / MCTrack / PointPillars 代码均不改动）。

---

## 七、风险与应对

| 风险 | 概率 | 应对 |
|------|------|------|
| GTSAM Python 绑定与 ROS Noetic 不兼容 | 中 | 改用 g2o 或纯 NumPy 实现 LM 优化 |
| MCTrack 速度传播误差累积过大 | 中 | 缩短窗口长度；改用每帧独立观测 |
| 目标观测噪声过大导致 ego 被拉偏 | 中 | 降低 λ；加强门控条件 |
| 实时性不满足 10Hz | 低 | 减小窗口大小（N=5）或跳帧处理 |

---

## 八、未来工作（方案 A 路线）

如果方案 B 验证有效，可进一步扩展为方案 A：

1. 将目标位姿也加入优化变量
2. 实现运动模型因子（目标相邻帧约束）
3. 实现刚体约束因子（LiDAR 点云形状一致性）
4. 输出 `/optimized_tracks` 提升跟踪精度
